int main() {
    // считываем число строк
    // создаём множество
    // заполняем его строками и выводим размер
    //int quantity; 
    set<string> words;
    string line;
    cin >> line;
    getline(cin, line);
    for (string str: line){cout << str << endl;}
    // при этом работает так : for (char str: line){cout << str << endl;}

    return 0;
}

читаю строку из ввода типа 6 cat dog cat fish dog dog и мне нужно закинуть все слова в set
